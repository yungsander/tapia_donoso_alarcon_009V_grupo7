server:
  port: 8088

spring:
  application:
    name: reportes
  datasource:
    url: jdbc:mysql://localhost:3306/db_reportes?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: aduana
    password: aduana123
  jpa:
    hibernate:
      ddl-auto: none

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true